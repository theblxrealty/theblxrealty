{
  "buildCommand": "pnpm --prefix client build",
  "installCommand": "pnpm --prefix client install",
  "framework": "nextjs",
  "outputDirectory": "client/.next",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["bom1"],
  "build": {
    "env": {
      "PRISMA_GENERATE_DATAPROXY": "true"
    }
  }
} 